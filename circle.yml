machine:
    services:
        - docker
    python:
        version: 2.7.12

dependencies:
    override:
        - pip install .
        - pip install pytest

test:
    override:
        - python setup.py test

deployment:
    master:
        branch: master
        heroku:
            appname: cs-standard-demo